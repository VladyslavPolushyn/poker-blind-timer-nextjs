import type { NextPage } from "next";
import styles from "../styles/HomePage.module.scss";

const Home: NextPage = () => {
  return (
    <div className={styles.HomePage}>
      <div className="container">TIMER COMPONENT</div>
    </div>
  );
};

export default Home;
